apiVersion: v1
kind: Service
metadata:
  name: {{ include "gorchestrator.fullname" . }}
  labels:
    {{- include "gorchestrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.mcpPort }}
    targetPort: mcp
    protocol: TCP
    name: mcp
  - port: {{ .Values.service.healthPort }}
    targetPort: health
    protocol: TCP
    name: health
  selector:
    {{- include "gorchestrator.selectorLabels" . | nindent 4 }}
